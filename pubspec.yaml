name: link_sdk
description: "A new Flutter plugin project."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  plugin:
    platforms:
      android:
        package: mobile.open.plugin.link_sdk
        pluginClass: LinkSdkPlugin
      ohos:
        pluginClass: LinkSdkPlugin
      ios:
        pluginClass: LinkSdkPlugin
